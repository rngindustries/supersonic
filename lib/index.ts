import { _short } from "./client";

const r = {
    commands: {},
    paths: {
        "commands": "./commands",
        "events": "./events",
        "middleware": "./middleware",
        "static": "./static"
    },
    short: _short
};

export default r;